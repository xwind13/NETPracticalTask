namespace NETPracticalTasks.ExternalLibraries;

internal class Archivator
{
    internal void Zip(string path, string archiveName, List<string> fileNames)
    {
        // имплементацияя логики для архивации списка файлов.
    }
}
